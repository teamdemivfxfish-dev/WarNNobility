# Gradle
.gradle/
build/

# NeoForge / moddev dev-run working dir
run/
runs/

# Build/IDE logs
*.log

# IntelliJ / Eclipse / VS Code
.idea/
*.iml
*.ipr
*.iws
.vscode/
.settings/
.classpath
.project
bin/
eclipse/
out/

# OS cruft
.DS_Store
Thumbs.db

# Local, machine-specific overrides (JDK path, deps_dir, etc.) — keep these out of the shared repo
gradle-local.properties

# Proprietary dependency jars are never committed (see README -> Building)
libs/
*.jar
# ...but the Gradle wrapper jar MUST be committed so the project builds out of the box
!gradle/wrapper/gradle-wrapper.jar
